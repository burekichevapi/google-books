export default class Book {
  constructor(id, title, authors, publisher, imageLink, linkToBook) {
    this.id = id;
    this.title = title;
    this.authors = authors;
    this.publisher = publisher;
    this.imageLink = imageLink;
    this.linkToBook = linkToBook;
  }
}
